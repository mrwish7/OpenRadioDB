const config = {
    db: {
        host: "localhost",
        user: "ordb_user",
        password: "ordb_user_pass",
        database: "ordb",
        connectTimeout: 60000
    },
    listPerPage: 50,
};
module.exports = config;